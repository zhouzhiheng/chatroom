server:
  port: 10005
spring:
  profiles:
    active: dev
  mvc:
    view:
      prefix: /WEB-INF/view/
      suffix: .jsp

# mybatis配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.chatroom.entity